The task
Using Visual Studio, create a project using the Console Application template.
Create two integer variables and give them some values. Using the technique of nested loops, draw a rectangle of stars. Use the values ​​of previously created variables to indicate the height and width of the rectangle.


Solution:

create a for loop; set the variable, condition and counter.
In the body of the loop, create another for loop. In the body of the letter indicate the output (*). In the main loop, after the nested for loop, we roll (*) to a new line. Display on screen result.